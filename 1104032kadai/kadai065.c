#include<stdio.h>
main()
{
	int i, a,sum;
	i = sum =a= 0;
	do
	{
		sum += a;
		printf("®”i|‚X‚X‚X‚ÅI—¹jH");
		scanf("%d", &a);
		i++;
	} while (a != -999);
	printf("‡Œv = %d\n•½‹Ï = %.2f", sum, (float)sum/(i-1));
}